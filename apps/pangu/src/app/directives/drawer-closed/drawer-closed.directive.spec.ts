import { DrawerClosedDirective } from './drawer-closed.directive';

describe('DrawerClosedDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerClosedDirective();
    expect(directive).toBeTruthy();
  });
});
